/**
 * SENERPOT — config.example.js
 * Plantilla de configuración local. Copiar a "config.js" y completar
 * con los valores reales antes de abrir index.html.
 *
 * config.js NO debe subirse al repositorio (ver .gitignore) — es lo que
 * antes estaba hardcodeado en texto plano dentro de index.html.
 */
const CONFIG = {
  apiUrl:  'https://script.google.com/macros/s/TU_ID_DE_DESPLIEGUE/exec',
  apiKey:  'TU_API_KEY_AQUI', // debe coincidir con API_KEY en Appscript/Api.gs.txt
  empresa: 'SENERPOT',
  version: '1.0'
};
